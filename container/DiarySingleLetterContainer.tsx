import React from 'react';

// component
import Layout from 'components/common/Layout/Layout';
import DiarySingleLetter from 'components/diary/letter';

const DiarySingleLetterContainer = () => {
  return (
    <Layout>
      <DiarySingleLetter />
    </Layout>
  );
};

export default DiarySingleLetterContainer;
